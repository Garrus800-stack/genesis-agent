## [7.9.42] — 2026-07-19

### Part A — The Run Healings

The first real life-run (two completed goals, eight idle thoughts, dream #1) yielded four precise findings — all four are healed. The sandbox read grant now resolves its root and compares on a separator boundary: a root containing spaces ("D:\Genesis Home") admits its children while the sibling "…Home2" stays out. A CODE step that returns no code field, and whose text does not look like code, no longer dies in the syntax parser — it is accepted as AMBIGUOUS text; real code (also arriving as output) keeps parsing and carries the parsat head on failure. At the continuation limit the partial result now travels honestly on the error (partialText, partialChars, continuationReason) — the pinned message stays, and nothing mistakes the fragment for finished code. And model selection has a memory: the last auto-choice rests in the soul (model-selection.json); a cold boot restores it instantly and lets the full scan run behind boot — the 9.9-second wait is gone, and without a memory, boot behaves exactly as before.

### Part B — The Nachklang (V2a, Genesis' own design)

"Das nehme ich mit" — when Genesis marks a moment itself, a new sibling of the v7.3.7 memory tools, `resonance-note`, condenses it immediately through one small structured model call into `{topic, stance, openQuestion}` and appends it to `.genesis/resonance.jsonl`. Nothing is ever written without an explicit mark. The idle mind picks it up as the PREFERRED topic source: the shared PickContext snapshot now carries the last condensates, and Reflect boosts strongly when they are present — while every other source stays exactly where it was, untouched. Tolerant by design: a missing or broken file degrades to silence, never to an error. And a small style healing (A5): announces must name the action, tool, or skill verbatim — the field run showed paraphrased announces drifting from what was then executed.

## Older versions

- [CHANGELOG-v7.md](CHANGELOG-v7.md) — full v7.x history
- [docs/CHANGELOG-v6.md](docs/CHANGELOG-v6.md) — v6.x history
- [docs/CHANGELOG-v5.md](docs/CHANGELOG-v5.md) — v5.x history
- [docs/CHANGELOG-archive.md](docs/CHANGELOG-archive.md) — v0.x–v4.x archive
